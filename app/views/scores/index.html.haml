%h1 History

%table.table.table-striped
  %thead
    %tr
      %th Date
      %th Opponent
      %th Score
      %th Result

  - @history.each do |score|
    %tr
      %td= score.date_played
      - r = score.results(current_user)
      %td= r[:adversary].name
      %td
        = r[:score]
        \-
        = r[:opponent_score]
      %td= r[:is_winner] ? 'W' : 'L'
